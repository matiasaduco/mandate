// Stage 7 — UI / feedback + loss check.
//
// Updates loss counters and emits GameOver when conditions are met.
// Skeleton in T-006; real logic lands in T-016 / T-017.

import type { EngineState } from '../types'
import type { EngineContext } from './context'

export function stage7_feedback(state: EngineState, _ctx: EngineContext): EngineState {
  return state
}
